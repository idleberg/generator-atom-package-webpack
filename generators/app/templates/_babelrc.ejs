{
    "presets": [
        [
            "@babel/preset-env", {
                "targets": {
                    "electron": "2.0.0",
                },
                "useBuiltIns": "entry",
            }
        ]<% pkg.babelPresets.forEach( preset => {  %>,
        <%- JSON.stringify(eslintPreset) %><% }) %>
    ],
}
